package main

import (
	// "github.com/eagledb14/paperlink/engagement"
	// "fmt"
	// "github.com/eagledb14/paperlink/types"
	// "fmt"

	// "github.com/eagledb14/paperlink/auth"
)

func main() {
	Run()
	// a := auth.NewAuth()
	// fmt.Println(a.NewUser("user", "pass", false))
	// u, e := a.NewUser("user", "pass", false)
	// a.InsertUser(u)
	// fmt.Println(u, e)
	//
	// hi, err := a.ValidateUser("user", "pass")
	// fmt.Println(hi, err)
	//
	// u, _ := auth.NewUser("user", "pass", false)
	// a.InsertUser(u)
	// // fmt.Println(u, err)
	// fmt.Println("usr", u.UserHash)
	// fmt.Println("pas", u.PassHash)
	// fmt.Println("salt", u.Salt)
	// fmt.Println("admin", u.Admin)
	// u2, err := a.GetUser(u.UserHash)
	// fmt.Println(u2.UserHash, err)
	// fmt.Println(u2.PassHash == u.PassHash)
}
